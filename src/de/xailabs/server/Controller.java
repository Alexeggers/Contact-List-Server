package de.xailabs.server;

public class Controller {
	
	private SQL sql;
	
	public void acceptCommand(CommandObject commandObject) {
		if (commandObject.getCommand().equals("New Contact")) {
			
		}
	}
}
